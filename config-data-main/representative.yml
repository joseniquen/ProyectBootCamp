server:
  port: 8098

spring:
  application:
    name: representative-microservice
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ProyectBootCamp

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:9999/eureka/
